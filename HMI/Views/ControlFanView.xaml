<UserControl x:Class="HMI.Views.ControlFanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HMI.Views"
                          xmlns:viewmodel="clr-namespace:HMI.ViewModels"
d:DataContext="{d:DesignInstance Type=viewmodel:CommandControlViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>


        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Style="{DynamicResource title}" Text="{Binding Title , FallbackValue=' ControlFan'}"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left"  >

            <Button x:Name ="TurnON" Command="{Binding TurnOnCommand }" Content="Turn on" Width="100" Height="40" Margin="0,0,10,0"/>
            <Button x:Name ="TurnOFF" Command="{Binding TurnOffCommand}" Content="Turn off" Width="100" Height="40"/>
            <TextBox Text="{Binding Value}" Width="80" Height="40" Margin="10"/>
            <Button  Command="{Binding SetSpeedCommand}" Content="SetSpeed" Width="100" Height="40"/>

        </StackPanel>

        

    </Grid>
</UserControl>
