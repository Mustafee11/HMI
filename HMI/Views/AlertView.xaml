<UserControl x:Class="HMI.Views.AlertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HMI.Views"
             xmlns:viewmodel="clr-namespace:HMI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:AlertViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            

        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Style="{DynamicResource title}" Text="{Binding Title , FallbackValue=' Alert'}"/>
           
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding AlertsItems}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="150" Header="Time" DisplayMemberBinding="{Binding TimeStamp}"/>
                    <GridViewColumn Width="80" Header="Severity" DisplayMemberBinding="{Binding Severity}"/>
                    <GridViewColumn Width="110" Header="Type" DisplayMemberBinding="{Binding Type}"/>
                    <GridViewColumn Width="150" Header="Machine" DisplayMemberBinding="{Binding Machine}"/>
                    <GridViewColumn Width="280" Header="Message" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
